<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotePad" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.NotePad" parent="Base.Theme.NotePad">
        <!-- Customize your light theme here. -->
        <item name="android:iconTint">@color/on_surface</item>
        <item name="android:textColor">@color/on_surface</item>
        <item name="android:tint">@color/on_surface</item>
        <item name="android:foregroundTint">@color/on_surface</item>
        <item name="android:fontFamily">@font/inter</item>
        <item name="android:background">@color/bg_surface</item>
    </style>
</resources>